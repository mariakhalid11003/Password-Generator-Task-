Password Generator

A Python-based password generator that allows users to customize:

- Password length
- Uppercase characters
- Lowercase characters
- Numbers
- Special characters

The program validates user input before generating the password.